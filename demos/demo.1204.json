{
  "input": "demos/inputs/funnel-hug-lanes.json",
  "caption": "Funnel lane-hugging around terrain, recorded on the optimized build. This clip exercises the exact PathField code the PR touches -- the corner-arrive test and the cell-proximity test in next_step -- so it is the behaviour most at risk from swapping distance_to for distance_squared_to. The route is unchanged: both comparisons square their threshold as well, which is exactly equivalent for non-negative operands.",
  "fixed_fps": 60,
  "max_frames": 600,
  "fps": 20,
  "width": 720
}
