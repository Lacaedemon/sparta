{
  "skip": true,
  "reason": "CI-hardening only: the demo-video workflow's tick parsers and the recorder now degrade (warn + default) instead of failing on a malformed state/frames/camera field. Nothing player-visible to film; the degrade path was live-tested on this PR's own demo run against a deliberately malformed input script (see the PR description for the green run + warnings)."
}
