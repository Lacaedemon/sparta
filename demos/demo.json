{
  "replay": "demos/scenarios/resizable-widths.json",
  "caption": "Resizable unit widths: a selected regiment is widened with ] then narrowed with [ -- the same soldiers reflowing from a wide, shallow line into a deeper column. The pressed keys flash on screen next to the cursor (a new keystroke overlay for demos), and the HUD reads out the live file count.",
  "fixed_fps": 60,
  "max_frames": 240,
  "fps": 30,
  "width": 720
}
