{
  "skip": true,
  "reason": "Hard per-soldier separation is a sub-pixel cosmetic correction inside the flock renderer; the effect (marks never overlap) is not distinguishable from the existing spring-separation in a recorded GIF at normal zoom."
}
