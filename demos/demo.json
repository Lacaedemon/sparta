{
  "skip": true,
  "reason": "Pure refactor + new pure helper functions (formation grid-ops). slots() output is byte-identical and the new transpose/widen/narrow helpers have no caller yet, so there is no visual change to show. The maneuvers that consume these (quarter-turn #371, widen, move-order wiring) will carry their own demos."
}
