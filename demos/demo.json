{
  "skip": true,
  "reason": "The speed label fix during wheeling requires '☰ Menu → Unit speed labels' to be toggled on (default off). A recorded battle clip can't flip that menu checkbox, so the corrected arc-speed reading wouldn't render in an unattended recording."
}
