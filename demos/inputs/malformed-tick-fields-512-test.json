{
  "_comment": "TEMPORARY live-CI fixture for the degrade-not-fail tick-parser fix: every optional array field here carries the wrong TYPE on purpose (camera: number, frames: bool, state: comma-string). The demo job must warn, fall back to defaults, and still record. Deleted before merge.",
  "seed": "12345",
  "camera": 5,
  "frames": true,
  "state": "8,60",
  "steps": [
    {"tick": 8, "click": [500, 300]},
    {"tick": 24, "rmb_drag": {"from": [620, 460], "to": [385, 460]}}
  ]
}
