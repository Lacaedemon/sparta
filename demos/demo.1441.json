{
  "input": "demos/inputs/routing-terrain-pathfinding.json",
  "caption": "Website demo catalog defect sweep, slice 1: a fighting unit whose morale is eroded to zero now breaks even when rank-cycle relief would have nudged it back above zero on the same tick, so the brittle cavalry routs around the hill as the clip declares; the other clips in this slice got re-pinned expect values and narrow declared exemptions.",
  "fixed_fps": 30,
  "max_frames": 400,
  "fps": 20,
  "width": 640
}
