{
  "skip": true,
  "reason": "Documentation and one guard test only. The diff touches the verify-via-state-dump and sparta-demos skills, a docstring in tools/demo/DemoState.gd, and test/unit/test_demo_state.gd -- no scenes/, scripts/, assets/ or project.godot change, so the running game is byte-identical and a recorded battle would show the same clip as main. The change being reviewed is that the checklist prose now points at DemoDefects.gd instead of restating a stale version of its tuning."
}
